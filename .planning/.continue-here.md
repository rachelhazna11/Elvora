---
context: phase
phase: 6
phase_name: Cart + Checkout
plan: null
status: not_started
last_updated: 2026-06-16T15:42:14.324Z
---

# Continue Here — Phase 06: Cart + Checkout

Phase 05 (AI Style Match) is complete as of 2026-06-16. This file marks the entry point for the next session.

## Human Actions Required Before Executing Phase 06

Complete these manually before running any code tasks:

- [ ] **Apply migration 005**: Open Supabase Dashboard → SQL Editor, run the full contents of `supabase/migrations/005_style_sessions.sql`. This creates the `ai_style_sessions` table with RLS policies. Without it, Style Match session persistence fails with a 42P01 error.
- [ ] **Set NVIDIA_API_KEY**: In Netlify → Site settings → Environment variables, add `NVIDIA_API_KEY` with your NVIDIA NIM key. `GEMINI_API_KEY` is no longer used and can be removed.

## What Was Completed Last Session (Phase 05)

| Commit | Description |
|--------|-------------|
| `02f084e` | feat(style-match): ganti Gemini ke NVIDIA NIM minimax/minimax-m3 |
| `ea537c2` | docs(phase-05): mark phase complete, update ROADMAP dan STATE |
| `46a763f` | fix(05): CORS multi-origin, CATALOG_CONTEXT 22 produk, response_mime_type JSON |
| `fdddd74` | docs(phase-05): update tracking after phase 05 wave completion |

Plans complete: 05-03 (style-match.html UI), 05-04 (js/style-match.js), 05-05 (polish + AI-USAGE.md).

## AI Style Match — Key Implementation Notes

`supabase/functions/style-match/index.ts`:
- CORS: dynamic `getCorsHeaders()` — allows `elvorastudio.netlify.app` AND `elvora.netlify.app`. Do NOT revert to single origin or wildcard.
- AI provider: NVIDIA NIM, endpoint `https://integrate.api.nvidia.com/v1/chat/completions`, model `minimax/minimax-m3`
- Photo: signed URLs are fetched server-side and re-encoded as base64 data URI before sending to NIM
- JSON: strips markdown fences from model output before `JSON.parse`
- Fallback: if `NVIDIA_API_KEY` absent, returns curated mock using real product UUIDs

## Next Steps

1. Complete human actions above (migration 005 + NVIDIA_API_KEY)
2. Run `/gsd-plan-phase 6` to generate Phase 06 plans (they do not exist yet)
3. Run `/gsd-execute-phase 6`

## Phase 06 Goal

A visitor — guest or authenticated — can add products to the cart, review and adjust the order, complete a multi-step checkout, and receive an order confirmation with a persisted order record.

Key complexity areas: guest-to-auth cart merge (localStorage → Supabase on login), order state machine with price snapshot, mini cart drawer Alpine.js component, multi-step checkout (Shipping → Review → Payment placeholder → Confirmation).
