---
phase: 05-ai-style-match
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified: [supabase/functions/style-match/index.ts]
autonomous: true
requirements: [F-033, F-034, F-049, NF-007]

must_haves:
  truths:
    - "Edge function returns valid JSON with recommendations and guidance"
    - "Edge function uses Gemini 1.5 Flash for analysis"
    - "Catalog context is scoped to the 22 hero products"
  artifacts:
    - path: "supabase/functions/style-match/index.ts"
      provides: "Gemini Vision integration"
---

<objective>
Implement the core AI logic in the Supabase Edge Function using Gemini 1.5 Flash.

Purpose: To provide intelligent activewear recommendations based on user photos and preferences.
Output: A functional `style-match` Edge Function that integrates with Gemini Vision.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-ai-style-match/05-CONTEXT.md
@.planning/phases/05-ai-style-match/05-RESEARCH.md
@supabase/functions/style-match/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Catalog Context and System Prompt</name>
  <files>supabase/functions/style-match/index.ts</files>
  <action>
    Create a constant `CATALOG_CONTEXT` containing the 22 hero products (ids, names, categories, descriptions, base_prices, primary_colors, and aesthetic_tags) derived from seed.sql.
    Define a `SYSTEM_PROMPT` for Elvy, the premium stylist. 
    The prompt must enforce:
    - Quiet luxury tone.
    - Analysis of the user's photo (coloring, build, style).
    - Mapping to 2–3 named outfit combinations (2–3 products each) from the catalog.
    - Providing 1–2 sentences of "Stylist's Note" guidance.
    - Strict JSON output format: { recommendations: [{ name, product_ids: [], colour_guidance }], guidance }.
  </action>
  <verify>
    <automated>grep -c "CATALOG_CONTEXT" supabase/functions/style-match/index.ts && grep -c "SYSTEM_PROMPT" supabase/functions/style-match/index.ts</automated>
  </verify>
  <done>Catalog and Prompt are defined in the edge function code.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Gemini Vision API Call</name>
  <files>supabase/functions/style-match/index.ts</files>
  <action>
    Replace the mock block with a real fetch call to the Gemini 1.5 Flash API.
    - Use `Deno.env.get('GEMINI_API_KEY')`.
    - Handle both photo-only, preference-only, and combined requests.
    - If a photo URL is provided, fetch the image (or use it as a URL if Gemini supports it directly, otherwise convert to base64).
    - Note: For MVP, assume the photo_url is a public or signed URL accessible by the function.
    - Implement a fallback to "Bestsellers" if the AI fails or can't find a match (per D-08).
    - Ensure CORS origin is locked to 'https://elvorastudio.netlify.app' (Actual) and 'https://elvora.netlify.app' (Planned).
  </action>
  <verify>
    <automated>grep -c "fetch(\"https://generativelanguage.googleapis.com" supabase/functions/style-match/index.ts</automated>
  </verify>
  <done>Edge function successfully communicates with Gemini API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→Edge Function | Untrusted preferences and photo URLs |
| Edge Function→Gemini | Catalog context and user data shared with third-party |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | Edge Function | mitigate | CORS locked to production domains in CORS_HEADERS |
| T-05-02-02 | Tampering | Edge Function | mitigate | JWT verification (Authorization header) required for all requests |
| T-05-02-03 | Denial of Service | Gemini API | mitigate | Fallback to static "Bestsellers" if API fails or times out |
</threat_model>

<verification>
Test the Edge Function locally using `supabase functions serve` and `curl`.
Verify that it returns a structured JSON matching the expected schema.
</verification>

<success_criteria>
- Edge Function returns a valid JSON recommendation based on a test photo/preferences.
- No GEMINI_API_KEY is hardcoded in the file.
- CORS and Auth checks are active.
</success_criteria>

<output>
Create `.planning/phases/05-ai-style-match/05-02-SUMMARY.md` when done
</output>
