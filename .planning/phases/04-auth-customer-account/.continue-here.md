---
context: phase
phase: 04-auth-customer-account
task: fix-stage
total_tasks: null
status: in_progress
last_updated: 2026-06-15T00:29:36.110Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints discovered this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Re-running reviewer unnecessarily | The 04-REVIEW.md already exists and is committed. Re-spawning gsd-code-reviewer wastes context and time. | advisory | Read REVIEW.md first; only re-review if findings seem stale or files changed since ee77d98. |

<current_state>
Phase 4 (auth-customer-account) is **fully complete** — all 5 plans executed, verified, and committed (latest: `4cbb497 docs(04): mark Phase 04 complete`).

A code review was run in a previous session and produced `.planning/phases/04-auth-customer-account/04-REVIEW.md` (committed `ee77d98`). The review found **3 Critical + 5 Warning + 3 Info** issues.

This session attempted `/gsd-code-review 4 --fix` to auto-apply fixes. The reviewer subagent hit the **daily session limit** and returned no output. The `--fix` (gsd-code-fixer) stage **never ran**.

The REVIEW.md on disk is valid and complete. The fix stage is the only remaining work.
</current_state>

<completed_work>

Phase 4 execution — all complete:
- Task 1: 04-01 Auth CSS components (src/input.css) — Done
- Task 2: 04-02 auth.js + components.js (auth logic + Alpine store) — Done
- Task 3: 04-03 auth.html split layout + tabbed forms — Done
- Task 4: 04-04 account.html dashboard + Supabase migration 003 — Done (`f408806`)
- Task 5: 04-05 Wishlist toggle shop.html + product.html — Done (`bed0062`)
- Code review (04-REVIEW.md) — Done (`ee77d98`)
</completed_work>

<remaining_work>

- **Fix stage**: Run gsd-code-fixer against the existing 04-REVIEW.md to auto-apply Critical + Warning fixes (CR-01 through WR-05)

Findings to fix:
  - **CR-01** (Critical): `window.elvoraSignOut` fragile global + no `.catch()` — `js/components.js:167`
  - **CR-02** (Critical): Missing `.auth-panel-content` CSS class breaks left-panel layout — `src/input.css:868`
  - **CR-03** (Critical): "Forgot password" link points to raw Supabase dashboard — `auth.html:126-129`
  - **WR-01** (Warning): `getUser()` destructure has no try/catch — `js/auth.js:43`
  - **WR-02** (Warning): `handleSignUp` skips empty name validation — `auth.html:58-78`
  - **WR-03** (Warning): Double-submit possible on profile/prefs forms — `account.html:160,203,259,308`
  - **WR-04** (Warning): `goToProduct(slug)` doesn't `encodeURIComponent` slug — `js/components.js:68`
  - **WR-05** (Warning): Auth subscription leak — `initAuth()` called twice — `js/components.js:26-29`
</remaining_work>

<decisions_made>

- CR-03 fix: use `supabase.auth.resetPasswordForEmail()` in an inline Alpine button — not a link to Supabase dashboard
- WR-05 fix: guard `initAuth()` with `let _authInitialized = false` module-level flag
- WR-01 fix: wrap `getUser()` in try/catch returning null on throw
- WR-03 fix: remove redundant `@click.prevent` from submit buttons; keep only `<form @submit.prevent>`
</decisions_made>

<blockers>
- Session limit hit during reviewer subagent — resolved by using existing REVIEW.md directly
</blockers>

## Required Reading (in order)
1. `.planning/phases/04-auth-customer-account/04-REVIEW.md` — full findings with exact file/line locations and code fixes
2. `$HOME/.claude/gsd-core/workflows/code-review-fix.md` — the fix workflow to execute

## Critical Anti-Patterns (do NOT repeat these)
- **DO NOT re-spawn gsd-code-reviewer** — REVIEW.md already exists and is committed. Go straight to the fix stage.

## Infrastructure State
- No dev servers running
- Supabase project: elvorastudio (free tier — may need manual resume if paused)
- Tailwind watch: not running (run `npx @tailwindcss/cli -i src/input.css -o src/output.css --watch` if editing CSS)

<context>
Phase 4 is done. The only open item is applying the code review fixes. The REVIEW.md file has precise file:line locations and exact code snippets for every fix — the gsd-code-fixer agent can work from it directly. After fixing, commit with `docs(04): apply code review fixes` and then move to Phase 5 planning.
</context>

<next_action>
Load `$HOME/.claude/gsd-core/workflows/code-review-fix.md` and execute the fix workflow against `.planning/phases/04-auth-customer-account/04-REVIEW.md`. This covers Critical + Warning findings (CR-01, CR-02, CR-03, WR-01 through WR-05). DO NOT re-run the reviewer.
</next_action>
