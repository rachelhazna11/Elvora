---
phase: 04-auth-customer-account
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/input.css
autonomous: true
requirements:
  - F-025
  - F-026
  - F-027
  - F-028
  - F-029

must_haves:
  truths:
    - "All Phase 4 CSS component classes exist in src/input.css — Tailwind CLI can compile them"
    - "Auth split layout classes (.auth-page, .auth-panel-left, .auth-panel-right, .auth-tabs, .auth-tab, .auth-form-wrap) are defined"
    - "Account dashboard layout classes (.account-page, .account-layout, .account-sidebar, .account-nav, .account-nav-item, .account-content, .account-section) are defined"
    - "Wishlist grid and card classes (.wishlist-grid, .wishlist-card, .wishlist-remove-btn, .wishlist-empty) are defined"
    - "Style preferences chip classes (.prefs-form, .prefs-field, .prefs-chips, .prefs-chip, .prefs-chip.selected) are defined"
    - "Nav logged-in dropdown classes (.nav-user-trigger, .nav-user-name, .nav-dropdown, .nav-dropdown-item, .nav-dropdown-item.danger, .nav-dropdown-divider) are defined"
    - ".wishlist-btn.active adds background: var(--rose); color: white"
    - "Responsive breakpoints at 768px collapse auth split and convert account sidebar to horizontal tab row"
  artifacts:
    - path: "src/input.css"
      provides: "All Phase 4 component CSS classes"
      contains: "/* ─── Auth + Account — Phase 4"
  key_links:
    - from: "src/input.css"
      to: "auth.html"
      via: "Tailwind CLI build → css/style.css"
      pattern: "\\.auth-page"
    - from: "src/input.css"
      to: "account.html"
      via: "Tailwind CLI build → css/style.css"
      pattern: "\\.account-layout"
---

<objective>
Add all Phase 4 CSS component classes to src/input.css under a new section header. This plan ships purely CSS — no HTML or JS changes. Every visual class that auth.html, account.html, and the wishlist heart toggle need must exist before those pages are built.

Purpose: Wave 1 foundation — auth.html (Plan 03) and account.html (Plan 04) can reference CSS classes by name without re-defining styles inline. Tailwind CLI picks up these @layer components entries at Netlify build time.
Output: src/input.css extended with ~60 new class definitions covering 7 component groups.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-auth-customer-account/04-UI-SPEC.md
@.planning/phases/04-auth-customer-account/04-PATTERNS.md
@src/input.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 4 auth + nav-dropdown CSS classes to src/input.css</name>
  <files>src/input.css</files>
  <read_first>
    - src/input.css — read lines 640–695 to see existing .form-group / .form-submit / .wishlist-btn patterns and the file end at line 852; append after line 852
  </read_first>
  <action>
    Append a new section block at the END of src/input.css (after the closing brace of the last @media block at line 852). The block starts with the separator comment and contains auth page classes and nav dropdown classes.

    Section header: /* ─── Auth + Account — Phase 4 ─────────────────────────────────────────── */

    Auth editorial split layout (all within @layer components or plain class definitions matching existing file style — look at how existing classes are defined and match exactly):

    .auth-page — height: 100vh; display: grid; grid-template-columns: 1fr 1fr; overflow: hidden
    .auth-panel-left — position: relative; overflow: hidden; background-size: cover; background-position: center
    .auth-panel-overlay — position: absolute; inset: 0; background: linear-gradient(to top, rgba(46,46,46,0.6) 0%, rgba(46,46,46,0.25) 100%)
    .auth-wordmark — position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); font-family: var(--font-display); font-size: 36px; font-weight: 700; font-style: italic; color: white; letter-spacing: 6px; white-space: nowrap
    .auth-tagline — display: block; text-align: center; margin-top: 12px; font-size: 11px; letter-spacing: 4px; text-transform: uppercase; color: rgba(255,255,255,0.7); font-family: var(--font-body)
    .auth-panel-right — background: var(--beige); display: flex; align-items: center; justify-content: center; padding: 48px; overflow-y: auto
    .auth-form-wrap — max-width: 400px; width: 100%
    .auth-tabs — display: flex; gap: 0; border-bottom: 1px solid rgba(46,46,46,0.1); margin-bottom: 32px
    .auth-tab — font-family: var(--font-body); font-size: 11px; font-weight: 400; letter-spacing: 2px; text-transform: uppercase; padding: 8px 0; margin-right: 24px; border: none; border-bottom: 2px solid transparent; cursor: pointer; color: var(--text-muted); background: none; transition: all 0.2s
    .auth-tab.active — color: var(--charcoal); border-bottom-color: var(--charcoal)
    .auth-form-title — font-family: var(--font-display); font-size: 22px; font-weight: 700; color: var(--charcoal); margin-bottom: 8px; line-height: 1.2
    .auth-form-sub — font-family: var(--font-body); font-size: 14px; font-weight: 400; color: var(--text-muted); margin-bottom: 32px; line-height: 1.6
    .auth-name-row — display: grid; grid-template-columns: 1fr 1fr; gap: 16px
    .auth-switch-link — font-family: var(--font-body); font-size: 14px; color: var(--text-muted); text-align: center; margin-top: 24px
    .auth-switch-link a — color: var(--charcoal); font-weight: 400; text-decoration: underline; text-underline-offset: 3px
    .auth-forgot — font-family: var(--font-body); font-size: 14px; color: var(--text-muted); text-decoration: underline; float: right; margin-bottom: 8px; cursor: pointer; background: none; border: none

    Nav logged-in dropdown classes:
    .nav-user-trigger — display: flex; align-items: center; gap: 8px; cursor: pointer; padding: 8px 16px; border-radius: 8px; background: none; border: none; transition: background 0.2s
    .nav-user-trigger:hover — background: rgba(46,46,46,0.06)
    .nav-user-name — font-family: var(--font-body); font-size: 14px; font-weight: 400; color: var(--charcoal); max-width: 100px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap
    .nav-dropdown — position: absolute; top: calc(100% + 8px); right: 0; background: white; border-radius: 16px; padding: 8px; box-shadow: 0 8px 32px rgba(0,0,0,0.1); min-width: 180px; z-index: 100
    .nav-dropdown-item — display: block; padding: 8px 16px; border-radius: 10px; font-family: var(--font-body); font-size: 14px; font-weight: 400; color: var(--charcoal); text-decoration: none; transition: background 0.2s; cursor: pointer; border: none; width: 100%; text-align: left; background: none
    .nav-dropdown-item:hover — background: var(--beige)
    .nav-dropdown-divider — height: 1px; background: rgba(46,46,46,0.08); margin: 4px 8px
    .nav-dropdown-item.danger — color: var(--rose)

    Also add .wishlist-btn.active at the end of the existing .wishlist-btn block area (after line 690): background: var(--rose); color: white

    Do NOT add any new CSS custom properties (no new :root variables). All values reference existing tokens (--beige, --sage, --rose, --charcoal, --text-muted, --font-body, --font-display, --rose-light, --sage-light).
  </action>
  <verify>
    <automated>grep -c "auth-page\|auth-panel-left\|auth-tab\|auth-form-wrap\|nav-user-trigger\|nav-dropdown\|nav-dropdown-item" /Users/andika/Desktop/Elvora/src/input.css</automated>
  </verify>
  <done>grep returns count >= 7 (one match per class group). auth-page, auth-panel-left, auth-tab, auth-form-wrap, nav-user-trigger, nav-dropdown, nav-dropdown-item all exist in src/input.css.</done>
</task>

<task type="auto">
  <name>Task 2: Add account dashboard + wishlist + preferences CSS classes to src/input.css</name>
  <files>src/input.css</files>
  <read_first>
    - src/input.css — read the tail after Task 1's additions to identify the append point; all classes go in the same Phase 4 section block
  </read_first>
  <action>
    Continue appending in the same Phase 4 section (after auth + nav classes from Task 1). Add the following class definitions:

    Account dashboard layout:
    .account-page — background: var(--beige); min-height: calc(100vh - 80px); padding-top: 80px
    .account-layout — display: grid; grid-template-columns: 240px 1fr; gap: 48px; max-width: 1200px; margin: 0 auto; padding: 48px 64px 100px
    .account-sidebar — background: white; border-radius: 20px; padding: 32px 24px; align-self: start; position: sticky; top: 104px
    .account-user-name — font-family: var(--font-display); font-size: 22px; font-weight: 700; color: var(--charcoal); margin-bottom: 4px; line-height: 1.2
    .account-user-email — font-family: var(--font-body); font-size: 14px; font-weight: 400; color: var(--text-muted); margin-bottom: 32px
    .account-nav — display: flex; flex-direction: column; gap: 4px
    .account-nav-item — font-family: var(--font-body); font-size: 11px; font-weight: 400; letter-spacing: 2px; text-transform: uppercase; padding: 8px 16px; border-radius: 10px; cursor: pointer; color: var(--text-muted); border-left: 2px solid transparent; border-top: none; border-right: none; border-bottom: none; background: none; transition: all 0.2s; width: 100%; text-align: left
    .account-nav-item.active — color: var(--charcoal); border-left-color: var(--rose); background: var(--rose-light)
    .account-nav-item:hover — color: var(--charcoal); background: var(--beige)
    .account-content — min-height: 400px
    .account-section — background: white; border-radius: 20px; padding: 48px; margin-bottom: 24px
    .account-section-title — font-family: var(--font-display); font-size: 22px; font-weight: 700; color: var(--charcoal); margin-bottom: 8px; line-height: 1.2
    .account-section-sub — font-family: var(--font-body); font-size: 14px; font-weight: 400; color: var(--text-muted); margin-bottom: 32px

    Profile section:
    .profile-form — max-width: 480px
    .profile-email-note — font-family: var(--font-body); font-size: 14px; color: var(--text-muted); font-style: italic; margin-top: 4px
    .profile-save-btn — max-width: 200px

    Wishlist section:
    .wishlist-grid — display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px
    .wishlist-card — position: relative
    .wishlist-remove-btn — position: absolute; top: 8px; right: 8px; z-index: 3; width: 32px; height: 32px; border-radius: 50%; background: white; border: 1px solid rgba(46,46,46,0.15); cursor: pointer; font-size: 14px; color: var(--text-muted); transition: all 0.2s; display: flex; align-items: center; justify-content: center; min-width: 44px; min-height: 44px
    .wishlist-remove-btn:hover — background: var(--rose); color: white; border-color: var(--rose)
    .wishlist-empty — min-height: 280px; flex-direction: column; gap: 16px
    .wishlist-empty-icon — font-size: 48px; color: rgba(168,191,163,0.4); display: block; text-align: center
    .wishlist-empty-cta — margin-top: 8px

    Style preferences section:
    .prefs-form — display: flex; flex-direction: column; gap: 32px
    .prefs-field — display: flex; flex-direction: column; gap: 8px
    .prefs-field-label — font-family: var(--font-body); font-size: 11px; font-weight: 400; letter-spacing: 2px; text-transform: uppercase; color: var(--text-muted)
    .prefs-chips — display: flex; gap: 8px; flex-wrap: wrap
    .prefs-chip — padding: 8px 24px; border-radius: 50px; border: 1.5px solid rgba(168,191,163,0.5); font-size: 14px; font-weight: 400; cursor: pointer; transition: all 0.2s; background: transparent; font-family: var(--font-body); color: var(--charcoal); min-height: 44px; display: inline-flex; align-items: center
    .prefs-chip.selected — background: var(--sage); color: white; border-color: var(--sage)
    .prefs-chip:hover:not(.selected) — border-color: var(--charcoal); color: var(--charcoal)
    .prefs-save-btn — max-width: 240px; margin-top: 8px

    Responsive rules — add an @media block for max-width: 1023px that:
    - .auth-page: grid-template-columns: 1fr (hides left panel)
    - .auth-panel-left: display: none
    - .auth-panel-right: padding: 48px 24px
    - .account-layout: grid-template-columns: 1fr; padding: 32px 24px 80px; gap: 24px
    - .account-sidebar: position: static; padding: 16px 0; border-radius: 0; background: transparent; border-bottom: 1px solid rgba(46,46,46,0.1); margin-bottom: 8px
    - .account-nav: flex-direction: row; overflow-x: auto; gap: 0; border-bottom: none; padding-bottom: 0
    - .account-nav-item: border-left: none; border-bottom: 2px solid transparent; padding: 8px 24px; white-space: nowrap; border-radius: 0
    - .account-nav-item.active: border-left-color: transparent; border-bottom-color: var(--rose); background: transparent
    - .wishlist-grid: grid-template-columns: repeat(2, 1fr)

    Add an @media block for max-width: 767px that:
    - .account-section: padding: 32px 24px
    - .wishlist-grid: grid-template-columns: 1fr
    - .account-layout: padding: 24px 16px 80px
  </action>
  <verify>
    <automated>grep -c "account-page\|account-layout\|account-nav-item\|wishlist-grid\|prefs-chip\|prefs-form\|account-section" /Users/andika/Desktop/Elvora/src/input.css</automated>
  </verify>
  <done>grep returns count >= 7. account-page, account-layout, account-nav-item, wishlist-grid, prefs-chip, prefs-form, account-section all exist in src/input.css. File is valid CSS (no syntax errors detectable by inspection).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file | Pure styling — no user input, no data processing, no trust boundary crossings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-CSS | Tampering | src/input.css | accept | CSS-only plan; no user data or auth logic; no security surface |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan; all CDN deps already in project from Phases 1–3 |
</threat_model>

<verification>
After both tasks complete:
1. grep -c "auth-page" src/input.css — must return >= 1
2. grep -c "account-layout" src/input.css — must return >= 1
3. grep -c "prefs-chip" src/input.css — must return >= 1
4. grep -c "nav-dropdown" src/input.css — must return >= 1
5. grep -c "wishlist-btn.active\|\.wishlist-btn\.active" src/input.css — must return >= 1
6. No new :root CSS variables added — grep ":root" count should not increase from baseline
7. File ends with valid closing brace; no truncation
</verification>

<success_criteria>
src/input.css contains all Phase 4 component classes enumerated in 04-UI-SPEC.md. Plans 02, 03, 04, 05 can reference any Phase 4 CSS class name without defining it inline. Tailwind CLI build at Netlify will include these classes because they appear in input.css under @layer components or as plain class definitions (whichever pattern is already in use in the file — match exactly).
</success_criteria>

<artifacts>
## Artifacts This Phase Produces

| Symbol | File | Type | Consumed By |
|--------|------|------|-------------|
| .auth-page | src/input.css | CSS class | auth.html |
| .auth-panel-left / .auth-panel-right | src/input.css | CSS class | auth.html |
| .auth-panel-overlay | src/input.css | CSS class | auth.html |
| .auth-wordmark / .auth-tagline | src/input.css | CSS class | auth.html |
| .auth-tabs / .auth-tab / .auth-tab.active | src/input.css | CSS class | auth.html |
| .auth-form-wrap / .auth-form-title / .auth-form-sub | src/input.css | CSS class | auth.html |
| .auth-name-row | src/input.css | CSS class | auth.html (signup grid) |
| .auth-switch-link / .auth-forgot | src/input.css | CSS class | auth.html |
| .nav-user-trigger / .nav-user-name | src/input.css | CSS class | js/components.js nav HTML |
| .nav-dropdown / .nav-dropdown-item / .nav-dropdown-item.danger / .nav-dropdown-divider | src/input.css | CSS class | js/components.js nav HTML |
| .wishlist-btn.active | src/input.css | CSS modifier | shop.html, product.html |
| .account-page / .account-layout | src/input.css | CSS class | account.html |
| .account-sidebar / .account-nav / .account-nav-item / .account-nav-item.active | src/input.css | CSS class | account.html |
| .account-content / .account-section / .account-section-title / .account-section-sub | src/input.css | CSS class | account.html |
| .account-user-name / .account-user-email | src/input.css | CSS class | account.html |
| .profile-form / .profile-email-note / .profile-save-btn | src/input.css | CSS class | account.html profile section |
| .wishlist-grid / .wishlist-card / .wishlist-remove-btn / .wishlist-empty / .wishlist-empty-icon / .wishlist-empty-cta | src/input.css | CSS class | account.html wishlist section |
| .prefs-form / .prefs-field / .prefs-field-label / .prefs-chips / .prefs-chip / .prefs-chip.selected / .prefs-save-btn | src/input.css | CSS class | account.html preferences section |
</artifacts>

<output>
Create `.planning/phases/04-auth-customer-account/04-01-SUMMARY.md` when done.
</output>
