---
phase: 01-foundation
plan: 06
type: execute
wave: 3
depends_on:
  - 01-04
  - 01-05
files_modified:
  - netlify.toml
  - README.md
  - .github/workflows/keepalive.yml
autonomous: false
requirements:
  - F-051
  - NF-006
  - NF-008

user_setup:
  - service: netlify
    why: "Deploy the site to Netlify and configure environment variables in the platform"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon (public) key"
    dashboard_config:
      - task: "Create Netlify site from GitHub repo"
        location: "app.netlify.com -> Add new site -> Import an existing project -> GitHub"
      - task: "Set environment variables"
        location: "Netlify site dashboard -> Site configuration -> Environment variables -> Add SUPABASE_URL and SUPABASE_ANON_KEY"
      - task: "Set GitHub Actions secret SUPABASE_URL"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"

must_haves:
  truths:
    - "netlify.toml has [build] with Tailwind CLI command and publish = '.'"
    - "README.md documents project setup, env vars, and deployment steps"
    - ".github/workflows/keepalive.yml pings SUPABASE_URL/auth/v1/health on a cron schedule"
    - "The deployed Netlify site returns a response at its public URL"
  artifacts:
    - path: "netlify.toml"
      provides: "Netlify build configuration"
      contains: "npx @tailwindcss/cli"
    - path: "README.md"
      provides: "Project setup documentation"
      contains: "## Setup"
    - path: ".github/workflows/keepalive.yml"
      provides: "GitHub Actions keep-alive ping"
      contains: "schedule:"
  key_links:
    - from: "netlify.toml [build] command"
      to: "css/style.css"
      via: "Tailwind CLI compiles src/input.css"
      pattern: "tailwindcss.*-i src/input.css.*-o css/style.css"
---

<objective>
Create the deployment pipeline: netlify.toml with the Tailwind build command, README.md with setup docs, and the GitHub Actions keep-alive workflow. Then trigger Netlify deployment and verify the site is publicly accessible.

Purpose: Per F-051 and NF-008, the app must be deployed to a public URL for assessment review. The keep-alive ping prevents the free Supabase project from pausing during the assessment period.

Output: Live Netlify URL, README.md, netlify.toml, .github/workflows/keepalive.yml.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create netlify.toml, README.md, and GitHub Actions keep-alive workflow</name>
  <files>netlify.toml, README.md, .github/workflows/keepalive.yml</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md lines 405–419 (netlify.toml pattern with build command and redirects)
    - .planning/phases/01-foundation/01-PATTERNS.md lines 421–440 (.github/workflows/keepalive.yml pattern with cron)
    - .planning/REQUIREMENTS.md lines 895–906 (F-051: Netlify deploy, env vars in platform, Tailwind CLI, keep-alive)
    - .planning/REQUIREMENTS.md lines 1039–1053 (NF-008: .env.example, no hardcoded localhost, reproducible build)
    - .planning/phases/01-foundation/01-CONTEXT.md lines 35–37 (D-10: .env gitignored, .env.example committed)
  </read_first>
  <action>
    Create netlify.toml at project root:
    - [build] section: command = "npx @tailwindcss/cli@next -i src/input.css -o css/style.css --minify", publish = "."
    - [build.environment] section: NODE_VERSION = "20"
    - [[redirects]] section: from = "/*", to = "/index.html", status = 200
    No hardcoded env var values in netlify.toml — Netlify platform env vars provide them.

    Create README.md with sections:
    - # Elvora — Premium Women's Activewear: brief project description (2–3 sentences), assessment context
    - ## Tech Stack: Alpine.js, Tailwind CSS v4, Supabase, Netlify Functions (as table or short list)
    - ## Setup: step-by-step — (1) clone repo, (2) create .env from .env.example, (3) add Supabase URL + anon key, (4) open index.html in browser or use live server
    - ## Database: apply supabase/migrations/001_schema.sql via Supabase SQL Editor; run supabase/seed.sql once
    - ## Build: npx @tailwindcss/cli@next -i src/input.css -o css/style.css (development); add --minify for production
    - ## Deployment: Netlify — connect repo, add SUPABASE_URL and SUPABASE_ANON_KEY in Netlify env vars, deploy
    - ## Environment Variables: table with SUPABASE_URL, SUPABASE_ANON_KEY, GEMINI_API_KEY (Supabase secret — not Netlify)
    - ## AI Usage: brief note pointing to AI-USAGE.md for full documentation (AI-USAGE.md created in Phase 5)

    Create .github/workflows/ directory and keepalive.yml:
    - name: "Supabase Keep-Alive"
    - on.schedule: cron '0 8 * * 1' (every Monday 08:00 UTC — weekly is sufficient for free-tier)
    - jobs.ping.runs-on: ubuntu-latest
    - steps: single step "Ping Supabase health" running curl -f "${{ secrets.SUPABASE_URL }}/auth/v1/health"
    - Add a comment above the curl step: "# Prevents Supabase free-tier project from pausing after 1 week of inactivity"
  </action>
  <verify>
    <automated>grep -c "tailwindcss" netlify.toml && grep -c "schedule:" .github/workflows/keepalive.yml && grep -c "## Setup" README.md</automated>
  </verify>
  <acceptance_criteria>
    - netlify.toml exists and contains "npx @tailwindcss/cli" in the build command
    - netlify.toml contains publish = "."
    - netlify.toml contains [[redirects]] block pointing /* to /index.html
    - netlify.toml does NOT contain any hardcoded SUPABASE_URL or SUPABASE_ANON_KEY values
    - README.md exists and contains "## Setup" section
    - README.md contains "SUPABASE_URL" and "SUPABASE_ANON_KEY" in the environment variables section
    - README.md does NOT contain "localhost" except inside setup instructions for local dev (not in any code path)
    - .github/workflows/keepalive.yml exists and contains "schedule:" with a cron expression
    - .github/workflows/keepalive.yml contains "auth/v1/health" in the curl command
    - .github/workflows/keepalive.yml uses "${{ secrets.SUPABASE_URL }}" not a hardcoded URL
  </acceptance_criteria>
  <done>netlify.toml configures Tailwind build and SPA redirects. README.md documents setup and deployment. keepalive.yml pings Supabase weekly on GitHub Actions schedule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Deploy to Netlify and verify public URL is live</name>
  <read_first>
    - netlify.toml (confirm build config before deploying)
    - .env.example (reference for which env vars to add in Netlify dashboard)
  </read_first>
  <what-built>netlify.toml configures the Tailwind build. The repository has all scaffold files (HTML shells, JS stubs, CSS source). The site is ready to deploy.</what-built>
  <how-to-verify>
    1. Push all Phase 1 code to the main branch of your GitHub repository (git add -A && git commit -m "feat(phase-1): foundation scaffold" && git push)
    2. Go to app.netlify.com -> Add new site -> Import an existing project -> Connect to GitHub
    3. Select the Elvora repository and branch (main)
    4. Build settings are auto-detected from netlify.toml — verify build command shows "npx @tailwindcss/cli@next -i src/input.css -o css/style.css --minify" and publish directory shows "."
    5. Before deploying, add environment variables: Site configuration -> Environment variables -> Add variable: SUPABASE_URL = (your Supabase project URL) and SUPABASE_ANON_KEY = (your anon key)
    6. Click Deploy site
    7. Wait for build to complete (1–3 minutes). Open the Netlify-assigned URL (e.g., https://elvora-xyz.netlify.app)
    8. Verify: the browser loads without a "page not found" error — an empty page (white/beige background) with no console errors is expected at this stage
    9. Add GitHub Actions secret: go to your GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret -> Name: SUPABASE_URL, Value: your Supabase URL
    10. Optionally rename the Netlify site to "elvora" in Netlify site settings for a cleaner URL
  </how-to-verify>
  <resume-signal>Type "deployed" and paste the live Netlify URL (e.g., https://elvora.netlify.app) once the site is live and loading without errors</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Netlify build → env vars | SUPABASE_URL and SUPABASE_ANON_KEY provided via Netlify platform env vars, not code |
| GitHub Actions → SUPABASE_URL | Secret stored in GitHub secrets, not in workflow YAML |
| Browser → Netlify CDN | Static HTML served over HTTPS; Netlify handles TLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Hardcoded keys in netlify.toml | mitigate | netlify.toml acceptance criteria explicitly checks no hardcoded key values; env vars set in Netlify platform UI |
| T-06-02 | Information Disclosure | SUPABASE_URL in keepalive.yml | mitigate | Uses ${{ secrets.SUPABASE_URL }} from GitHub secrets, not hardcoded in YAML |
| T-06-03 | Denial of Service | Supabase free-tier pause | mitigate | Weekly cron ping to /auth/v1/health prevents project pausing during assessment period |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | netlify.toml build uses npx for one-time Tailwind CLI run — no lockfile dependency to audit |
</threat_model>

<verification>
After deployment checkpoint:
- curl -I {netlify-url} returns HTTP 200
- Browser opens {netlify-url} with no console errors
- Netlify dashboard shows "Published" status for the latest deploy
- GitHub Actions shows keepalive.yml in .github/workflows/
</verification>

<success_criteria>
netlify.toml, README.md, and keepalive.yml are created and committed. Netlify site is deployed with a public URL. Environment variables are configured in Netlify platform. GitHub Actions secret is set for the keep-alive workflow.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Description |
|---------------|------|-------------|
| `netlify.toml` | Config | Build command (Tailwind CLI), publish dir, SPA redirect rule |
| `README.md` | Documentation | Project setup, env vars, database, build, deployment instructions |
| `.github/workflows/keepalive.yml` | GitHub Actions | Weekly cron pinging Supabase /auth/v1/health |
| Live Netlify URL | Deployment | Public URL for assessment demo |
