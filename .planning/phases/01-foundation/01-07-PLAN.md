---
phase: 01-foundation
plan: 07
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-02
files_modified: []
autonomous: false
requirements:
  - F-050

user_setup:
  - service: supabase
    why: "Seed data must be applied manually via Supabase SQL Editor (cloud-only dev per D-08)"
    dashboard_config:
      - task: "Apply seed.sql"
        location: "Supabase Dashboard -> SQL Editor -> New Query — paste full contents of supabase/seed.sql and click Run"
      - task: "Verify 20+ products exist"
        location: "Supabase Dashboard -> Table Editor -> products — check row count"

must_haves:
  truths:
    - "20+ products exist in the Supabase products table"
    - "product_variants table has rows with colour and size data for each product"
    - "product_images table has at least 2 Unsplash URL rows per product"
    - "testimonials table has at least 5 rows"
    - "categories and collections tables are populated"
  artifacts:
    - path: "supabase/seed.sql"
      provides: "Applied — data now in live Supabase project"
  key_links:
    - from: "supabase/seed.sql"
      to: "Live Supabase tables: products, product_variants, product_images, categories, collections, testimonials, reviews"
      via: "Manual paste into Supabase SQL Editor"
      pattern: "products table row count >= 20"
---

<objective>
Apply the seed data from Plan 03 to the live Supabase project via the SQL Editor dashboard. This blocking checkpoint ensures the product catalog is populated before Phase 2 renders it.

Purpose: Cloud-only development (D-08) means no CLI push — the human must paste and execute seed.sql in the Supabase dashboard. Downstream phases cannot render real products without this step.

Output: Live Supabase project with 20+ products, variants, images, categories, collections, testimonials, and sample reviews.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply supabase/seed.sql to Supabase via SQL Editor</name>
  <read_first>
    - supabase/seed.sql (the full SQL to paste)
    - .planning/phases/01-foundation/01-CONTEXT.md lines 33–36 (D-08: cloud-only, SQL editor only)
  </read_first>
  <what-built>Plan 03 created supabase/seed.sql with 22+ products, variants, images, categories, collections, testimonials, and reviews.</what-built>
  <how-to-verify>
    1. Open your Supabase project dashboard
    2. Navigate to: SQL Editor -> New Query
    3. Open supabase/seed.sql in your editor and copy the full contents
    4. Paste into the SQL Editor and click "Run"
    5. Confirm: no errors in the result panel
    6. Navigate to Table Editor -> products: verify at least 20 rows exist
    7. Navigate to Table Editor -> product_variants: verify rows exist with colour and size columns populated
    8. Navigate to Table Editor -> product_images: verify at least 40 rows exist (2+ per product)
    9. Navigate to Table Editor -> testimonials: verify at least 5 rows exist
    10. Navigate to Table Editor -> categories: verify 6 rows exist (leggings, sports-bra, top, jacket, shorts, set)
  </how-to-verify>
  <resume-signal>Type "seed applied" once you have confirmed products, variants, images, and testimonials are visible in the Supabase Table Editor</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human → Supabase SQL Editor | Only the project owner applies seed data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Seed data visibility | accept | All seeded data is catalog content (products, reviews, testimonials) — no PII, no credentials |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Confirmed via Supabase Table Editor:
- products table: >= 20 rows
- product_variants table: >= 40 rows
- product_images table: >= 40 rows with Unsplash URLs
- testimonials table: >= 5 rows
- categories table: 6 rows
</verification>

<success_criteria>
Live Supabase project contains 20+ seeded products with variants, images, categories, collections, reviews, and testimonials. The catalog is ready for Phase 2's homepage and Phase 3's product listing.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-07-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Description |
|---------------|------|-------------|
| Live product catalog (22+ products) | Supabase data | Products, variants, images, categories, collections in live DB |
| Live testimonials (5+) | Supabase data | Testimonials for homepage Phase 2 |
| Live reviews (24+) | Supabase data | Product reviews for Phase 3 PDP |
