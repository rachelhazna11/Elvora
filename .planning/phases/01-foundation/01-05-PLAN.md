---
phase: 01-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/style-match/index.ts
autonomous: true
requirements:
  - F-049
  - NF-005

must_haves:
  truths:
    - "supabase/functions/style-match/index.ts exists with JWT validation, CORS headers, mock response, and request logging"
    - "CORS origin is locked to the production Netlify domain (not wildcard *)"
    - "The function returns the exact mock JSON shape that Phase 5 will replace with real Gemini output"
    - "The GEMINI_API_KEY is NOT referenced in the function body (stub does not call Gemini)"
  artifacts:
    - path: "supabase/functions/style-match/index.ts"
      provides: "Auth-validated Supabase Edge Function stub"
      contains: "Authorization"
      min_lines: 50
  key_links:
    - from: "supabase/functions/style-match/index.ts"
      to: "Supabase Edge Function runtime (Deno)"
      via: "supabase functions deploy style-match (manual deploy via Supabase dashboard)"
      pattern: "serve\\(async"
---

<objective>
Create the Supabase Edge Function stub for `style-match` — a Deno-based function that validates JWTs, accepts the Phase 5 request shape, returns a hardcoded mock response, and has production-safe CORS configuration.

Purpose: Per D-12 through D-15, Phase 1 deploys a working stub so the Edge Function infrastructure is live and verified before Phase 5 implements the real Gemini Vision call. The stub shape must exactly match Phase 5's expected output.

Output: `supabase/functions/style-match/index.ts` — ready to deploy via Supabase dashboard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create style-match Edge Function stub (Deno)</name>
  <files>supabase/functions/style-match/index.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 856–872 (F-049: JWT verification, CORS to production domain, GEMINI_API_KEY in secrets, request shape, mock response)
    - .planning/phases/01-foundation/01-PATTERNS.md lines 340–390 (exact stub code pattern with CORS headers, JWT check, mock response shape)
    - .planning/phases/01-foundation/01-CONTEXT.md lines 39–55 (D-12 through D-15: stub behaviour, mock response JSON, CORS lock)
  </read_first>
  <action>
    Create supabase/functions/ and supabase/functions/style-match/ directories, then create index.ts.

    Imports: import { serve } from "https://deno.land/std@0.168.0/http/server.ts" and import { createClient } from "https://esm.sh/@supabase/supabase-js@2".

    CORS_HEADERS constant: "Access-Control-Allow-Origin" set to "https://elvora.netlify.app" (NOT wildcard), "Access-Control-Allow-Headers" set to "authorization, x-client-info, apikey, content-type".

    Function body structure:
    1. Handle OPTIONS preflight: return 200 "ok" with CORS_HEADERS
    2. Check Authorization header — if absent, return 401 JSON { error: "Unauthorized" } with CORS headers
    3. Parse request body as JSON — expected shape: { photo_url: string | null, preferences: { activity: string[], fit: string, aesthetic: string, colour: string } }
    4. Log request with console.log("style-match request:", JSON.stringify({ photo_url: body.photo_url ? "[photo present]" : null, preferences: body.preferences })) — do NOT log full photo URL to avoid leaking signed URLs in logs
    5. Return hardcoded mock response matching D-14 exactly:
       { recommendations: [{ name: "Mock Outfit 1", product_ids: ["seed-product-id-1", "seed-product-id-2"], colour_guidance: "Earthy tones complement your natural colouring — lean into sage and ivory." }] }
    6. Response status 200, Content-Type application/json, with CORS_HEADERS

    Add a comment at top of file: "// Phase 1 stub — returns mock response. Phase 5 replaces the mock block with real Gemini Vision API call."
    Add a comment before the mock block: "// TODO: Phase 5 — replace this block with Gemini Vision API call using Deno.env.get('GEMINI_API_KEY')"

    The string "GEMINI_API_KEY" must only appear in that TODO comment — not in Deno.env.get() in this stub. The real key reference is added in Phase 5 only.
  </action>
  <verify>
    <automated>grep -c "Authorization" supabase/functions/style-match/index.ts && grep -c "elvora.netlify.app" supabase/functions/style-match/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - supabase/functions/style-match/index.ts exists with at least 50 lines
    - File contains "Authorization" (JWT header check)
    - File contains "elvora.netlify.app" (CORS locked to production domain — not *)
    - File contains "colour_guidance" in the mock response (exact Phase 5 response field)
    - File contains "product_ids" in the mock response
    - File contains "serve(async" (Deno serve pattern)
    - grep -c "Deno.env.get" returns 0 — GEMINI_API_KEY is NOT called in stub
    - grep for "*" as CORS origin value returns 0 — wildcard CORS is NOT used
    - File contains "// TODO: Phase 5" comment above mock block
  </acceptance_criteria>
  <done>supabase/functions/style-match/index.ts is a working auth-validated Deno stub: rejects unauthenticated requests, logs request shape, returns the exact mock JSON format Phase 5 will replace, and has CORS locked to the production domain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Edge Function | JWT required; unauthenticated requests rejected at function entry |
| Edge Function → Gemini API | Key stored in Supabase secrets only (Phase 5 adds the real call) |
| CORS | Origin locked to https://elvora.netlify.app — no wildcard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | JWT validation | mitigate | Authorization header checked at function entry; 401 returned if absent; Phase 5 adds full JWT verification via Supabase client |
| T-05-02 | Information Disclosure | CORS wildcard | mitigate | CORS origin locked to https://elvora.netlify.app in CORS_HEADERS constant — never * |
| T-05-03 | Information Disclosure | Request logging | mitigate | Photo URL is logged as "[photo present]" not the full signed URL; preferences are safe to log (no PII) |
| T-05-04 | Information Disclosure | GEMINI_API_KEY in code | mitigate | Key reference appears only in a TODO comment; actual Deno.env.get('GEMINI_API_KEY') call is deferred to Phase 5; grep check in acceptance criteria enforces this |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Deno runtime uses URL imports — no npm install step |
</threat_model>

<verification>
- grep -c "Authorization" supabase/functions/style-match/index.ts returns >= 1
- grep -c "elvora.netlify.app" returns >= 1
- grep -c "Deno.env.get" returns 0 (key not referenced in stub)
- grep for wildcard CORS (*) as origin value returns 0
- File line count >= 50
</verification>

<success_criteria>
supabase/functions/style-match/index.ts is created with the Phase 1 stub behaviour: JWT auth gate, CORS locked to production, request logging, mock response matching the Phase 5 contract.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Description |
|---------------|------|-------------|
| `supabase/functions/style-match/index.ts` | Deno Edge Function | Auth-validated stub with CORS, request logging, mock Phase 5 response shape |
| `CORS_HEADERS` | Constant | Production-locked CORS config (origin: https://elvora.netlify.app) |
