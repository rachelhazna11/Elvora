---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - shop.html
  - product.html
  - cart.html
  - checkout.html
  - auth.html
  - account.html
  - style-match.html
  - about.html
  - contact.html
  - admin.html
  - js/supabase.js
  - js/auth.js
  - js/cart.js
  - js/products.js
  - js/style-match.js
  - js/admin.js
  - src/input.css
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - NF-006
  - NF-008

must_haves:
  truths:
    - "All 11 HTML page shells exist with correct head boilerplate and no inline JS"
    - "js/supabase.js exports a single supabase client — all stub JS files import from it"
    - "src/input.css declares @theme with all 8 colour tokens and 2 font tokens"
    - ".env.example documents SUPABASE_URL and SUPABASE_ANON_KEY with placeholder values"
    - ".gitignore excludes .env and node_modules"
  artifacts:
    - path: "js/supabase.js"
      provides: "Shared Supabase client singleton"
      contains: "createClient"
    - path: "src/input.css"
      provides: "Tailwind v4 source with brand tokens"
      contains: "@theme"
    - path: ".env.example"
      provides: "Environment variable documentation"
      contains: "SUPABASE_URL="
  key_links:
    - from: "js/supabase.js"
      to: "All other js/*.js stubs"
      via: "import { supabase } from './supabase.js'"
      pattern: "import.*supabase"
    - from: "src/input.css"
      to: "css/style.css"
      via: "Tailwind CLI build (netlify.toml)"
      pattern: "@import \"tailwindcss\""
---

<objective>
Create the complete repository scaffold: 11 HTML page shells, 6 JS module stubs, Tailwind v4 CSS source with brand tokens, environment variable template, and gitignore.

Purpose: Per D-06, Phase 1 establishes the full directory structure so all downstream phases fill content without restructuring. Every HTML page, JS module, and CSS file must exist now.

Output: 19 files created — the structural backbone all future phases build into.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 11 HTML page shells and Tailwind CSS source</name>
  <files>index.html, shop.html, product.html, cart.html, checkout.html, auth.html, account.html, style-match.html, about.html, contact.html, admin.html, src/input.css</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md lines 146–199 (HTML shell boilerplate pattern, exact head structure)
    - .planning/phases/01-foundation/01-PATTERNS.md lines 239–279 (src/input.css pattern — @import, @theme, :root, body)
    - .planning/phases/01-foundation/01-CONTEXT.md lines 26–30 (D-04, D-05, D-06, D-07: repo structure decisions)
    - .planning/phases/01-foundation/01-PATTERNS.md lines 40–58 (colour tokens from prototype)
  </read_first>
  <action>
    Create each HTML file following the exact boilerplate from PATTERNS.md: DOCTYPE, html lang="en", head with charset, viewport, title "ELVORA — [Page Name]", Google Fonts link (Playfair Display + Poppins), link to /css/style.css. Body with single main containing a comment "<!-- Phase X fills this -->", then script type="module" src="/js/supabase.js" at end of body. No inline styles, no inline scripts (NF-006).

    Page-specific titles and phase comments:
    - index.html: "ELVORA — Home", "<!-- Phase 2 fills this -->"
    - shop.html: "ELVORA — Shop", "<!-- Phase 3 fills this -->"
    - product.html: "ELVORA — Product", "<!-- Phase 3 fills this -->"
    - cart.html: "ELVORA — Cart", "<!-- Phase 6 fills this -->"
    - checkout.html: "ELVORA — Checkout", "<!-- Phase 6 fills this -->"
    - auth.html: "ELVORA — Sign In", "<!-- Phase 4 fills this -->"
    - account.html: "ELVORA — My Account", "<!-- Phase 4 fills this -->"
    - style-match.html: "ELVORA — Style Match", "<!-- Phase 5 fills this -->"
    - about.html: "ELVORA — About", "<!-- Phase 2 fills this -->"
    - contact.html: "ELVORA — Contact", "<!-- Phase 2 fills this -->"
    - admin.html: "ELVORA — Admin", "<!-- Phase 7 fills this -->"

    Create src/ directory and src/input.css with:
    - @import "tailwindcss"; at top
    - @theme block declaring all 8 colour custom properties: --color-sage: #A8BFA3, --color-rose: #D8A7A7, --color-beige: #F5F1ED, --color-charcoal: #2E2E2E, --color-lavender: #C6B7E2, --color-sage-light: #E8F0E6, --color-rose-light: #F5E8E8, --color-text-muted: #8A8A8A
    - @theme block also declaring --font-display: 'Playfair Display', serif and --font-body: 'Poppins', sans-serif
    - :root block mirroring as native CSS custom properties (--sage, --rose, --beige, --charcoal, --lavender, --sage-light, --rose-light, --text-muted) for use in non-Tailwind CSS
    - body rule: font-family: var(--font-body); background-color: var(--color-beige); color: var(--color-charcoal); overflow-x: hidden;
    - html rule: scroll-behavior: smooth;
    - * rule: box-sizing: border-box;
  </action>
  <verify>
    <automated>grep -l "Playfair Display" index.html shop.html product.html cart.html checkout.html auth.html account.html style-match.html about.html contact.html admin.html | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 11 HTML files exist in project root
    - grep -c "inline" combined across all HTML = 0 (no inline scripts or styles)
    - grep -c "Playfair Display" across all HTML files = 11
    - grep -c 'type="module"' across all HTML files = 11 (each page loads supabase.js as module)
    - grep -c "script" across all HTML files equals 11 (exactly one script tag per file — only supabase.js)
    - src/input.css exists and contains "@import \"tailwindcss\""
    - src/input.css contains "@theme" block
    - src/input.css contains all 8 colour hex values: #A8BFA3, #D8A7A7, #F5F1ED, #2E2E2E, #C6B7E2, #E8F0E6, #F5E8E8, #8A8A8A
    - src/input.css contains --font-display and --font-body declarations
  </acceptance_criteria>
  <done>All 11 HTML shells exist with correct boilerplate. src/input.css has brand tokens in @theme and :root. No inline JS anywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Create JS module stubs, .env.example, and .gitignore</name>
  <files>js/supabase.js, js/auth.js, js/cart.js, js/products.js, js/style-match.js, js/admin.js, .env.example, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md lines 200–236 (js/supabase.js pattern and stub pattern for other JS files)
    - .planning/phases/01-foundation/01-PATTERNS.md lines 393–399 (.env.example pattern)
    - .planning/phases/01-foundation/01-CONTEXT.md lines 35–36 (D-10: .env gitignored, .env.example committed)
    - .planning/phases/01-foundation/01-CONTEXT.md lines 26–30 (D-05: no inline JS, ES modules)
  </read_first>
  <action>
    Create js/ directory. Create js/supabase.js as the shared client following the PATTERNS.md pattern exactly: import createClient from CDN ESM URL https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm, read SUPABASE_URL from import.meta.env?.VITE_SUPABASE_URL ?? window.__ENV?.SUPABASE_URL, read SUPABASE_ANON_KEY from import.meta.env?.VITE_SUPABASE_ANON_KEY ?? window.__ENV?.SUPABASE_ANON_KEY, export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY). Add a comment "// Service role key MUST NOT appear here — RLS enforces access control."

    Create stub files — each imports supabase from './supabase.js' and exports named async functions with // TODO: Phase N comments:
    - js/auth.js: exports signUp, signIn, signOut, getUser, onAuthChange (// TODO: Phase 4)
    - js/cart.js: exports getCart, addToCart, removeFromCart, updateQuantity, clearCart (// TODO: Phase 6)
    - js/products.js: exports getProducts, getProduct, getCategories, searchProducts (// TODO: Phase 3)
    - js/style-match.js: exports submitStyleMatch, getSessions (// TODO: Phase 5)
    - js/admin.js: exports adminGetProducts, adminCreateProduct, adminUpdateProduct, adminDeleteProduct (// TODO: Phase 7)

    Create .env.example (committed to git):
    SUPABASE_URL=https://your-project-ref.supabase.co
    SUPABASE_ANON_KEY=your-anon-key-here
    # SUPABASE_SERVICE_ROLE_KEY — NEVER put this in any file. Use Supabase dashboard only.
    # GEMINI_API_KEY — stored in Supabase Edge Function secrets only. NEVER here.

    Create .gitignore:
    - .env
    - node_modules/
    - css/style.css (generated — not committed; Netlify builds it)
    - .DS_Store
    - *.log
  </action>
  <verify>
    <automated>grep -c "createClient" js/supabase.js && grep -c "import.*supabase" js/auth.js js/cart.js js/products.js js/style-match.js js/admin.js</automated>
  </verify>
  <acceptance_criteria>
    - js/supabase.js contains "createClient(" and exports "export const supabase"
    - js/supabase.js does NOT contain "service_role" or "SERVICE_ROLE" (case-insensitive grep returns 0)
    - js/auth.js, js/cart.js, js/products.js, js/style-match.js, js/admin.js all exist and contain "import { supabase } from './supabase.js'"
    - All 5 stub files contain at least one "// TODO: Phase" comment
    - .env.example exists and contains "SUPABASE_URL=" and "SUPABASE_ANON_KEY="
    - .env.example does NOT contain any real key values (only placeholder strings)
    - .gitignore exists and contains ".env" (so .env is never committed)
    - .gitignore contains "node_modules/" and "css/style.css"
  </acceptance_criteria>
  <done>js/ directory has 6 ES module files — supabase.js is the singleton client, the other 5 are stubs importing from it. .env.example documents required env vars. .gitignore prevents .env from being committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → js/supabase.js | Only ANON_KEY is loaded client-side; RLS enforces data access |
| .env → git | .gitignore prevents .env from being committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | js/supabase.js anon key | accept | Anon key is safe to expose by design; RLS prevents unauthorized data access (per CLAUDE.md) |
| T-04-02 | Information Disclosure | .env committed to git | mitigate | .gitignore includes .env; .env.example committed instead with placeholder values only |
| T-04-03 | Information Disclosure | SERVICE_ROLE_KEY in JS | mitigate | Comment in js/supabase.js explicitly notes service role key must never appear here; grep check in acceptance criteria enforces this |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — CDN ESM imports only |
</threat_model>

<verification>
- grep -rn "service_role" js/ returns 0 results
- grep -c "createClient" js/supabase.js returns 1
- grep -rn "import.*supabase.*from.*supabase.js" js/ returns 5 (one per stub file)
- wc -l .gitignore shows .env is listed
- .env.example has no real key values
</verification>

<success_criteria>
All 11 HTML shells, 6 JS modules (1 real + 5 stubs), src/input.css with brand tokens, .env.example, and .gitignore are created. Repository structure matches D-04 through D-07 exactly.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Description |
|---------------|------|-------------|
| `index.html` through `admin.html` (11 files) | HTML shell | Boilerplate pages with correct head, font links, CSS link, module script |
| `js/supabase.js` | ES module | Shared Supabase client singleton — exports `supabase` |
| `js/auth.js` | ES module stub | Auth function stubs — exports signUp, signIn, signOut, getUser, onAuthChange |
| `js/cart.js` | ES module stub | Cart function stubs |
| `js/products.js` | ES module stub | Product query stubs |
| `js/style-match.js` | ES module stub | Style match stubs |
| `js/admin.js` | ES module stub | Admin CRUD stubs |
| `src/input.css` | Tailwind v4 source | Brand tokens in @theme, :root, base body styles |
| `.env.example` | Config template | Documents SUPABASE_URL, SUPABASE_ANON_KEY |
| `.gitignore` | Config | Excludes .env, node_modules, css/style.css |
