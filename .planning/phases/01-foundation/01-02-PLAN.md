---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements:
  - F-046
  - F-047
  - F-048
  - NF-005

user_setup:
  - service: supabase
    why: "Schema must be applied manually via Supabase SQL Editor (cloud-only dev per D-08)"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon (public) key"
    dashboard_config:
      - task: "Apply 001_schema.sql"
        location: "Supabase Dashboard -> SQL Editor -> New Query — paste full contents of supabase/migrations/001_schema.sql and click Run"
      - task: "Verify 16 tables exist"
        location: "Supabase Dashboard -> Table Editor — all 16 tables should be listed"
      - task: "Verify storage buckets exist"
        location: "Supabase Dashboard -> Storage — product-images and user-uploads buckets should be visible"

must_haves:
  truths:
    - "All 16 tables are visible in the Supabase Table Editor"
    - "RLS is shown as enabled on every table in Supabase Auth settings"
    - "product-images (public) and user-uploads (private) buckets appear in Supabase Storage"
    - "The is_admin() function exists in Supabase Database -> Functions"
  artifacts:
    - path: "supabase/migrations/001_schema.sql"
      provides: "Applied schema — tables, RLS, policies, buckets exist in live Supabase project"
  key_links:
    - from: "supabase/migrations/001_schema.sql"
      to: "Live Supabase PostgreSQL project"
      via: "Manual paste into Supabase SQL Editor"
      pattern: "all 16 tables visible in Table Editor"
---

<objective>
Apply the migration SQL from Plan 01 to the live Supabase project via the SQL Editor dashboard. This blocking checkpoint ensures the schema is live before seed data is inserted in Plan 03.

Purpose: Cloud-only development (D-08) means no CLI push — the human must paste and execute the SQL in the Supabase dashboard. No subsequent plan can succeed without this step.

Output: Live Supabase project with 16 tables, RLS, policies, is_admin() function, and two storage buckets.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Apply 001_schema.sql to Supabase via SQL Editor</name>
  <read_first>
    - supabase/migrations/001_schema.sql (the full SQL to paste)
    - .planning/phases/01-foundation/01-CONTEXT.md lines 33–36 (D-08: cloud-only, no CLI)
  </read_first>
  <what-built>Plan 01 created supabase/migrations/001_schema.sql with all 16 tables, RLS on every table, all RLS policies, the is_admin() function, and storage bucket configuration.</what-built>
  <how-to-verify>
    1. Open your Supabase project dashboard at https://supabase.com/dashboard
    2. Navigate to: SQL Editor -> New Query
    3. Open supabase/migrations/001_schema.sql in your editor and copy the entire file contents
    4. Paste into the Supabase SQL Editor and click "Run"
    5. Confirm: the bottom panel shows "Success. No rows returned" (or similar — no errors)
    6. Navigate to Table Editor — verify all 16 tables are listed: user_profiles, categories, products, product_variants, product_images, collections, collection_products, cart_items, wishlist_items, orders, order_items, reviews, testimonials, newsletter_subscribers, ai_style_sessions, promo_codes
    7. Navigate to Storage — verify two buckets exist: product-images (public) and user-uploads (private)
    8. Navigate to Database -> Functions — verify is_admin() function is listed
    9. Note your Project URL and anon key from Project Settings -> API (needed for .env in Plan 04)
  </how-to-verify>
  <resume-signal>Type "schema applied" once all 16 tables, both storage buckets, and is_admin() function are confirmed visible in the Supabase dashboard</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human → Supabase SQL Editor | Only the project owner applies schema; no automated SQL execution against production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | SQL Editor access | accept | Dashboard access requires Supabase account login; schema applied by owner only |
| T-02-02 | Tampering | RLS disabled tables | mitigate | Verify all 16 tables show RLS enabled in dashboard after apply; if any show disabled, re-run alter statement for that table |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Manual verification in Supabase dashboard:
- Table Editor shows exactly 16 tables
- Storage shows product-images (public=true) and user-uploads (public=false)
- Database -> Functions shows is_admin() with security definer
- Authentication -> Policies shows policies on every table
</verification>

<success_criteria>
All 16 tables exist in live Supabase project with RLS enabled. Both storage buckets exist with correct public/private settings. is_admin() function is deployed. The project is ready to receive seed data.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Symbol / File | Type | Description |
|---------------|------|-------------|
| Live Supabase tables (16) | Supabase PostgreSQL | All application tables live in project |
| Live storage buckets (2) | Supabase Storage | product-images (public), user-uploads (private) |
| is_admin() | Live PostgreSQL function | Admin role check deployed to project |
