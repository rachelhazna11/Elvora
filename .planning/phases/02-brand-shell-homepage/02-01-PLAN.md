---
phase: 02-brand-shell-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/input.css
  - js/components.js
  - shop.html
  - product.html
  - cart.html
  - checkout.html
  - auth.html
  - account.html
  - style-match.html
  - admin.html
autonomous: true
requirements:
  - NF-004
  - NF-003
  - F-045

must_haves:
  truths:
    - "Any HTML page that includes components.js shows the shared nav and footer"
    - "The nav scrolls from transparent to frosted glass on index.html after 80px"
    - "D-04: The mobile hamburger opens a full-height right slide-out drawer with all nav links, implemented with Alpine.js x-show + translate-x transition"
    - "Pressing Escape or clicking the backdrop closes the mobile drawer"
    - "src/input.css contains .btn-primary, .btn-secondary, .section-label, .section-title, and .marquee-track definitions"
    - "Alpine.store('cart') and Alpine.store('auth') are registered with their shape before Alpine processes x-data"
    - "All placeholder HTML stubs (shop, product, cart, checkout, auth, account, style-match, admin) have nav-root and footer-root divs plus correct script load order"
  artifacts:
    - path: "js/components.js"
      provides: "Nav + footer HTML injection, Alpine store registration, active nav link detection"
      exports: []
    - path: "src/input.css"
      provides: "Design system — component utilities (.btn-primary, .btn-secondary, .section-label, .section-title) and marquee keyframes"
      contains: ".btn-primary"
    - path: "shop.html"
      provides: "Placeholder shell with nav-root, footer-root, correct script order"
      contains: "nav-root"
  key_links:
    - from: "js/components.js"
      to: "js/supabase.js"
      via: "ES module import"
      pattern: "import.*supabase.*from.*supabase\\.js"
    - from: "Any HTML page"
      to: "js/components.js"
      via: "script type=module"
      pattern: "src=\"/js/components\\.js\""
    - from: "js/components.js"
      to: "Alpine.store"
      via: "alpine:init event listener"
      pattern: "alpine:init"
---

## Phase Goal

**As a** visitor, **I want to** land on a complete premium editorial homepage and navigate to brand content pages, **so that** I experience the quiet luxury identity and am drawn into the catalog.

<objective>
Establish the shared infrastructure that every subsequent Phase 2 plan builds on: the design system component utilities in src/input.css, the js/components.js module (nav + footer injection, Alpine store stubs, active link detection), and the updated HTML placeholders on all 8 non-primary pages.

Purpose: Plan 02 and 03 cannot start building page content until the nav/footer are injectable and the design system utilities exist. This wave-1 plan unblocks all parallel wave-2 work.

Output: js/components.js (new), src/input.css (extended), all placeholder HTML shells updated with nav-root/footer-root/correct script order. A visitor on any page sees the Elvora nav and footer.
</objective>

<execution_context>
@/Users/andika/.claude/gsd-core/workflows/execute-plan.md
@/Users/andika/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/andika/Desktop/Elvora/.planning/PROJECT.md
@/Users/andika/Desktop/Elvora/.planning/ROADMAP.md
@/Users/andika/Desktop/Elvora/.planning/phases/02-brand-shell-homepage/02-CONTEXT.md
@/Users/andika/Desktop/Elvora/.planning/phases/02-brand-shell-homepage/02-RESEARCH.md
@/Users/andika/Desktop/Elvora/.planning/phases/02-brand-shell-homepage/02-PATTERNS.md
@/Users/andika/Desktop/Elvora/.planning/phases/02-brand-shell-homepage/02-UI-SPEC.md
@/Users/andika/Desktop/Elvora/src/input.css
@/Users/andika/Desktop/Elvora/js/supabase.js
@/Users/andika/Desktop/Elvora/elvora_updated-3.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend src/input.css with component utilities and marquee keyframes</name>
  <files>src/input.css</files>
  <read_first>
    - src/input.css — read the full file; do NOT touch the existing @theme block or :root block; add only after the existing content
    - .planning/phases/02-brand-shell-homepage/02-RESEARCH.md — "Custom Component Utilities" section and "Pattern 6: Marquee Strip" for exact CSS values
    - .planning/phases/02-brand-shell-homepage/02-PATTERNS.md — "src/input.css" section for the exact @layer components and keyframes to add
    - elvora_updated-3.html — inline &lt;style&gt; block for the scrollAnim keyframes (lines ~198–203)
  </read_first>
  <action>
    Append to src/input.css (after the existing :root and body blocks, do NOT redefine any @theme or :root tokens):

    1. Add @layer components block containing exactly these utilities (values from 02-PATTERNS.md):
       - .section-label: font-size 10px, letter-spacing 5px, text-transform uppercase, color var(--color-sage), font-weight 500
       - .section-title: font-family var(--font-display), font-size clamp(32px,4vw,52px), font-weight 700, line-height 1.15, color var(--color-charcoal)
       - .btn-primary: background var(--color-charcoal), color white, padding 16px 36px, border-radius 9999px, font-size 12px, letter-spacing 2px, text-transform uppercase, font-weight 500, transition all 0.3s, font-family var(--font-body), text-decoration none, display inline-block, border none, cursor pointer
       - .btn-primary:hover: background var(--color-rose), transform translateY(-2px)
       - .btn-secondary: background transparent, color var(--color-charcoal), padding 16px 36px, border 1.5px solid var(--color-charcoal), border-radius 9999px, font-size 12px, letter-spacing 2px, text-transform uppercase, font-weight 500, transition all 0.3s, font-family var(--font-body), text-decoration none, display inline-block, cursor pointer
       - .btn-secondary:hover: border-color var(--color-rose), color var(--color-rose)

    2. Add three @keyframes declarations outside the @layer blocks:
       - @keyframes marquee: from translateX(0) to translateX(-50%)
       - .marquee-track: animation marquee 20s linear infinite
       - @media (prefers-reduced-motion: reduce): .marquee-track { animation: none }
       - @keyframes scrollAnim: 0% transform scaleY(0) transform-origin top; 50% scaleY(1) top; 51% scaleY(1) bottom; 100% scaleY(0) bottom

    Do NOT use fenced code blocks in this action. The implementation uses plain CSS property/value pairs described above. Use var(--color-*) references, never raw hex values.
  </action>
  <verify>
    <automated>grep -c "\.btn-primary" /Users/andika/Desktop/Elvora/src/input.css</automated>
  </verify>
  <acceptance_criteria>
    - src/input.css contains ".btn-primary {" and ".btn-secondary {"
    - src/input.css contains ".section-label {" and ".section-title {"
    - src/input.css contains "@keyframes marquee {" and ".marquee-track {"
    - The existing @theme block (--color-sage, --color-rose, etc.) is unchanged
    - The existing :root block and body block are unchanged
    - No raw hex values appear in the new additions (all use var(--color-*) references)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create js/components.js with nav, footer, Alpine stores, and active link detection</name>
  <files>js/components.js</files>
  <read_first>
    - js/supabase.js — import pattern; note the export name "supabase"
    - .planning/phases/02-brand-shell-homepage/02-RESEARCH.md — Pattern 1 (components.js full structure), Pattern 4 (nav scroll transparency), Common Pitfalls 1 (store registration), 5 (innerHTML + Alpine), and the full navHTML and footerHTML string examples
    - .planning/phases/02-brand-shell-homepage/02-PATTERNS.md — js/components.js section for Alpine store registration, nav x-data pattern, active link detection pattern
    - .planning/phases/02-brand-shell-homepage/02-UI-SPEC.md — Navigation component spec (nav links, icons, footer columns), Copywriting Contract (footer tagline, copyright), Interaction Contracts (nav scroll, mobile drawer)
    - elvora_updated-3.html — nav and footer sections for visual reference on SVG icons and link structure
  </read_first>
  <action>
    Create js/components.js as an ES module. Structure exactly as follows:

    1. Import supabase: `import { supabase } from './supabase.js';`

    2. Register Alpine stores inside `document.addEventListener('alpine:init', () => { ... })`:
       - Alpine.store('cart', { count: 0, items: [] })
       - Alpine.store('auth', { user: null, loggedIn: false })

    3. Define navHTML as a const string containing a complete &lt;nav&gt; element with:
       - x-data containing navOpen (false), scrolled (false), and an init() method that adds a passive window scroll listener setting scrolled = window.scrollY > 80 (per D-05)
       - :class binding: when scrolled → 'bg-beige/90 backdrop-blur-md border-b border-sage/20', else 'bg-transparent'
       - class: fixed top-0 left-0 right-0 z-[100] flex items-center justify-between px-12 py-5 transition-all duration-300
       - Logo: anchor href="/index.html" with text "ELVORA" using font-display font-bold text-2xl tracking-widest text-charcoal; include a simple SVG flower/dot glyph for the O (from prototype reference)
       - Desktop nav links (hidden md:flex gap-9 list-none): New Arrivals (/index.html), Collections (/shop.html), Style Match (/style-match.html), About (/about.html), Contact (/contact.html) — each with data-page attribute matching href, nav-link class, text-[12px] font-medium tracking-[2px] uppercase text-charcoal hover:text-rose transition-colors
       - Nav actions div (flex items-center gap-5):
         * Search button: aria-label="Search", 44×44px touch target (w-11 h-11 flex items-center justify-center), inline SVG search icon
         * User link to /auth.html: aria-label="Your account", same sizing
         * Cart link to /cart.html: aria-label="Shopping bag", relative positioning, cart badge span with x-show="$store.cart.count > 0" x-text="$store.cart.count" absolute positioning (-top-1 -right-1) w-4 h-4 rounded-full bg-rose text-white text-[9px]
         * Hamburger button: @click="navOpen = true", aria-label="Open navigation menu", md:hidden, 3-bar SVG
       - Mobile drawer div: x-show="navOpen", x-transition enter/leave with translate-x-full/translate-x-0, @keydown.escape.window="navOpen = false", fixed inset-y-0 right-0 w-72 bg-charcoal z-[200] flex flex-col p-8, style="display:none;"; contains close button (aria-label="Close navigation menu", self-end mb-8 text-white X SVG) and ul with all nav links in white text-white text-base tracking-widest uppercase
       - Backdrop div: x-show="navOpen", @click="navOpen = false", fixed inset-0 bg-charcoal/50 z-[150], style="display:none;"

    4. Define footerHTML as a const string containing a complete &lt;footer&gt; element with:
       - bg-charcoal text-white pt-[72px] pb-10
       - max-w-[1280px] mx-auto px-16
       - Top grid: grid grid-cols-1 md:grid-cols-4 gap-12 mb-16
         * Col 1 (brand, md:col-span-1): "ELVORA" wordmark in font-display font-bold text-xl tracking-widest, tagline from UI-SPEC copywriting ("Where movement meets elegance. Premium activewear for the modern woman."), 3 social icon buttons (40×40px, border border-white/20 rounded-full, hover text-rose transition-colors) for Instagram, Twitter/X, Pinterest
         * Col 2 "SHOP": links to New Arrivals, Best Sellers, Collections, Style Match
         * Col 3 "HELP": links to Size Guide, FAQ (/contact.html), Shipping & Returns, Contact (/contact.html)
         * Col 4 "BRAND": links to Our Story (/about.html), Sustainability, Press, Careers
         * All link text: text-sm text-white/45 hover:text-rose transition-colors font-light (per UI-SPEC)
         * Column titles: text-[11px] tracking-[4px] uppercase text-white font-medium mb-6
       - Bottom row: border-t border-white/10 pt-8 flex flex-col md:flex-row items-center justify-between gap-4
         * Copyright: "© 2026 Elvora. All rights reserved." — text-sm text-white/30
         * Links: Privacy Policy, Terms — text-sm text-white/30 hover:text-rose

    5. Inject nav and footer after the HTML strings:
       ```
       const navRoot = document.getElementById('nav-root');
       const footerRoot = document.getElementById('footer-root');
       if (navRoot) { navRoot.innerHTML = navHTML; if (window.Alpine) Alpine.initTree(navRoot); }
       if (footerRoot) { footerRoot.innerHTML = footerHTML; if (window.Alpine) Alpine.initTree(footerRoot); }
       ```

    6. Set active nav link: read window.location.pathname, call document.querySelectorAll('[data-page]').forEach(); for each link, compare href to currentPath (normalize /index.html → / comparison); if active, add class 'text-rose' (per 02-PATTERNS.md active link detection pattern)

    Critical constraints (per D-06, D-07):
    - Do NOT fetch data from Supabase in this file in Phase 2 (supabase is imported but only used by Alpine inline components in index.html in later plans)
    - Alpine stores are stubbed with empty/null defaults — NOT wired to real auth or cart in Phase 2
    - The nav DOES NOT show dynamic auth state or cart count dynamically in Phase 2 (badge only shows when $store.cart.count > 0, which is 0 by default)
  </action>
  <verify>
    <automated>grep -c "alpine:init" /Users/andika/Desktop/Elvora/js/components.js</automated>
  </verify>
  <acceptance_criteria>
    - js/components.js exists and exports nothing (side-effect module)
    - js/components.js contains "import { supabase } from './supabase.js';"
    - js/components.js contains "document.addEventListener('alpine:init',"
    - js/components.js contains "Alpine.store('cart'," and "Alpine.store('auth',"
    - js/components.js contains "getElementById('nav-root')" and "getElementById('footer-root')"
    - js/components.js contains "navOpen" in the navHTML string (per D-04 Alpine drawer)
    - js/components.js contains "window.scrollY > 80" (per D-05 scroll transparency)
    - js/components.js contains "data-page" attribute usage for active link detection
    - navHTML string contains "aria-label" on search, user, bag, and hamburger buttons
    - navHTML string contains "@keydown.escape.window" for keyboard close
    - footerHTML string contains "© 2026 Elvora"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update all HTML stubs with nav-root, footer-root, Alpine CDN, and correct script load order</name>
  <files>
    index.html, shop.html, product.html, cart.html, checkout.html, auth.html, account.html, style-match.html, admin.html
  </files>
  <read_first>
    - index.html — current stub to understand existing boilerplate; note missing Alpine CDN, missing nav-root, wrong font weights
    - shop.html — representative placeholder stub to see current state
    - .planning/phases/02-brand-shell-homepage/02-PATTERNS.md — "Placeholder HTML stubs" section for the exact boilerplate to apply, including admin.html exception (no footer-root)
    - .planning/phases/02-brand-shell-homepage/02-RESEARCH.md — Pattern 8 for load order, Pitfall 7 for correct Google Fonts URL
  </read_first>
  <action>
    Update all 9 HTML files to the correct boilerplate. For each file, the head must contain:
    - meta charset UTF-8
    - meta viewport content="width=device-width, initial-scale=1.0"
    - title tag with page-appropriate title using the "Page Title — ELVORA" format (see UI-SPEC copywriting contract for homepage, about, contact; use descriptive titles for stubs: "Shop — ELVORA", "Product — ELVORA", etc.)
    - Two &lt;link rel="preconnect"&gt; tags: https://fonts.googleapis.com and https://fonts.gstatic.com crossorigin
    - Google Fonts link with ONLY: family=Playfair+Display:ital,wght@0,700;1,700&family=Poppins:wght@400;600&display=swap (per Pitfall 7 — remove weights 300, 400i, 500 that were in the old URL)
    - &lt;link rel="stylesheet" href="/css/style.css"&gt;
    - Alpine CDN: &lt;script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.15.12/dist/cdn.min.js"&gt;&lt;/script&gt;

    The body must contain:
    - &lt;div id="nav-root"&gt;&lt;/div&gt; as the first element
    - &lt;main&gt; with existing content preserved (stubs keep their placeholder comment; index.html keeps "Phase 2 fills this" until Plan 02)
    - &lt;div id="footer-root"&gt;&lt;/div&gt; as the last element BEFORE scripts (EXCEPTION: admin.html omits footer-root — admin panel has no public footer)
    - Scripts in this exact order: &lt;script src="/js/__env.js"&gt;&lt;/script&gt;, &lt;script type="module" src="/js/supabase.js"&gt;&lt;/script&gt;, &lt;script type="module" src="/js/components.js"&gt;&lt;/script&gt;

    For index.html specifically: the &lt;main&gt; block currently says "Phase 2 fills this" — keep that placeholder text; Plan 02 will fill it. Do not add content yet.

    For admin.html specifically: omit &lt;div id="footer-root"&gt;&lt;/div&gt; (admin has no public footer per 02-PATTERNS.md).

    Important per D-07: the nav-root and footer-root placeholders are what allow components.js to inject the shared nav and footer. These divs must be present on every page including stubs.
  </action>
  <verify>
    <automated>grep -l "id=\"nav-root\"" /Users/andika/Desktop/Elvora/shop.html /Users/andika/Desktop/Elvora/product.html /Users/andika/Desktop/Elvora/cart.html /Users/andika/Desktop/Elvora/checkout.html /Users/andika/Desktop/Elvora/auth.html /Users/andika/Desktop/Elvora/account.html /Users/andika/Desktop/Elvora/style-match.html /Users/andika/Desktop/Elvora/admin.html /Users/andika/Desktop/Elvora/index.html</automated>
  </verify>
  <acceptance_criteria>
    - All 9 HTML files contain id="nav-root"
    - All 9 HTML files except admin.html contain id="footer-root"
    - All 9 HTML files contain the Alpine CDN script tag with defer attribute and version 3.15.12
    - All 9 HTML files have the script load order: __env.js first (plain script), then supabase.js (type=module), then components.js (type=module)
    - All 9 HTML files use the corrected Google Fonts URL (Playfair 700/700i + Poppins 400/600 only — no weights 300 or 500)
    - index.html retains its &lt;main&gt; placeholder content (not filled yet — that's Plan 02)
    - admin.html does NOT contain id="footer-root"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase | All Supabase reads/writes go through the anon key + RLS; no service_role key in browser |
| user input → DOM | The nav/footer HTML is a static developer-authored string; no user input is interpolated into innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | js/components.js navHTML string | accept | Nav/footer HTML is a static string authored at build time — no user-controlled values are interpolated. XSS via innerHTML is not applicable here. |
| T-02-02 | Information Disclosure | js/supabase.js (anon key in browser) | accept | Supabase anon key is safe to expose by design — RLS policies enforce data access. service_role key is never in any client file (verified: not present in any .js file). |
| T-02-03 | Denial of Service | newsletter_subscribers table | accept | Unique constraint on email prevents duplicate data. Rate limiting is Supabase/Netlify infrastructure concern, not Phase 2 code scope. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages installed in this plan. Tailwind and Alpine are CDN-loaded or already in package.json from Phase 01 (legitimacy audited in 02-RESEARCH.md). |
</threat_model>

<verification>
After all tasks complete:

1. Run Tailwind build and check output: `cd /Users/andika/Desktop/Elvora && npx @tailwindcss/cli -i src/input.css -o css/style.css 2>&1 | tail -5` — should complete without errors
2. Check .btn-primary appears in compiled CSS: `grep -c "btn-primary" /Users/andika/Desktop/Elvora/css/style.css`
3. Check all pages have nav-root: `grep -l "nav-root" /Users/andika/Desktop/Elvora/*.html | wc -l` — should return 9
4. Check components.js imports supabase: `grep "import.*supabase" /Users/andika/Desktop/Elvora/js/components.js`
5. Check Alpine CDN present: `grep -l "alpinejs@3.15.12" /Users/andika/Desktop/Elvora/*.html | wc -l` — should return 9
6. Verify no Google Fonts weight 300 in any HTML: `grep -l "wght@300\|wght@.*300" /Users/andika/Desktop/Elvora/*.html` — should return nothing
</verification>

<success_criteria>
- src/input.css contains .btn-primary, .btn-secondary, .section-label, .section-title, @keyframes marquee, .marquee-track
- js/components.js exists with: supabase import, alpine:init store registration, navHTML string (with scroll behavior + mobile drawer), footerHTML string, injection logic, active link detection
- All 9 HTML pages have nav-root div, Alpine CDN (defer), correct Google Fonts URL (Playfair 700+Poppins 400/600 only), and script load order (__env.js → supabase.js → components.js)
- admin.html has nav-root but NOT footer-root
- Tailwind CLI compiles src/input.css to css/style.css without errors
</success_criteria>

<output>
Create `.planning/phases/02-brand-shell-homepage/02-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New files:
- `js/components.js` — ES module; exports nothing (side-effect); contains `navHTML` string, `footerHTML` string, Alpine store registration (`Alpine.store('cart', {...})`, `Alpine.store('auth', {...})`), injection functions, active nav link detection

Modified files:
- `src/input.css` — adds @layer components: `.btn-primary`, `.btn-secondary`, `.section-label`, `.section-title`; adds @keyframes: `marquee`, `scrollAnim`; adds `.marquee-track` utility class
- `index.html`, `shop.html`, `product.html`, `cart.html`, `checkout.html`, `auth.html`, `account.html`, `style-match.html`, `admin.html` — all updated with `<div id="nav-root">`, `<div id="footer-root">` (except admin.html), Alpine CDN `defer` script, corrected Google Fonts URL, and script load order

Symbols created in js/components.js:
- `navHTML` (const string)
- `footerHTML` (const string)
</output>
